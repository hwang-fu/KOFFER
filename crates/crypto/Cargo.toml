[package]
name = "koffer-crypto"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
repository.workspace = true

[features]
default = []
std = ["alloc"]
alloc = []

[dependencies]
base = { package = "koffer-base", path = "../base" }
